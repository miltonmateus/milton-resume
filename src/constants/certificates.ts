import type { Certificate } from '../types/resume';

export const efSetCertificate: Certificate = {
  title: 'EF SET English Certificate',
  issuer: 'EF SET',
  credentialUrl: 'https://cert.efset.org/saRMra',
};
